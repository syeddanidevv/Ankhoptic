import Cart from "@/components/store/Cart";

export const metadata = {
  title: "Shopping Cart — Ankhoptics",
  description: "Review your cart and proceed to checkout.",
};

export default function CartPage() {
  return <Cart />;
}
